# V85.17 REV4 — Homologação

Correção focada no Monitoramento de Coletas.

- Corrige `v8517BulkState is not defined`.
- Move o fluxo `v8517Bulk*` para o mesmo escopo de `V`, `load()` e `$`.
- Mantém seleção individual, exibidos, todos filtrados, limpar seleção e excluir selecionados usando o mesmo estado.
- Preserva as correções da REV3 (`summary` nulo e `import copy`).
- Sem alteração das regras financeiras ou da programação recorrente.
