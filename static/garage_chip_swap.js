const $=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let data=[];
function fill(id,vals,label){$(id).innerHTML=`<option value="">${label}</option>`+[...new Set(vals.filter(Boolean))].sort().map(v=>`<option>${esc(v)}</option>`).join('')}
async function load(){let j=await fetch('/api/garage-chip-swaps',{cache:'no-store'}).then(r=>r.json());data=j.rows||[];fill('gCompany',data.map(x=>x.company),'Todas');fill('gModel',data.map(x=>x.model),'Todos');render()}
function render(){let c=$('gCompany').value,q=$('gTerminal').value.trim().toLowerCase(),m=$('gModel').value,s=$('gStatus').value,rows=data.filter(x=>(!c||x.company===c)&&(!q||x.terminal.toLowerCase().includes(q))&&(!m||x.model===m)&&(!s||x.status===s));let total=rows.length,done=rows.filter(x=>x.status==='CONCLUÍDA').length,prog=rows.filter(x=>x.status==='EM ANDAMENTO').length,pend=total-done-prog,pct=total?done*100/total:0;$('gTotal').textContent=total;$('gDone').textContent=done;$('gProg').textContent=prog;$('gPend').textContent=pend;$('gPct').textContent=pct.toFixed(1)+'%';$('gBar').style.width=Math.min(100,pct)+'%';$('gRows').innerHTML=rows.map(x=>`<article class="chipValidatorCard ${x.status==='CONCLUÍDA'?'done':''}"><div class="chipValidatorHead"><div><small>${esc(x.company)}</small><h3>Terminal ${esc(x.terminal)}</h3><p>${esc(x.model||'Modelo não informado')}${x.technician?' · '+esc(x.technician):''}</p></div><span class="tag">${esc(x.status)}</span></div><div class="chipEvidence">${(x.photos||[]).map(p=>`<figure class="chipEvidenceItem"><a href="${esc(p.url)}" target="_blank"><img src="${esc(p.thumb_url||p.url)}" loading="lazy"></a></figure>`).join('')}</div><form class="gForm" data-id="${x.id}"><label>Resultado após troca *</label><select name="test_result" required><option value="">Selecione</option><option value="TESTADO_OK" ${x.test_result==='TESTADO_OK'?'selected':''}>Testado - OK</option><option value="TESTADO_COM_DEFEITO" ${x.test_result==='TESTADO_COM_DEFEITO'?'selected':''}>Testado - com defeito</option><option value="NAO_FOI_POSSIVEL_TESTAR" ${x.test_result==='NAO_FOI_POSSIVEL_TESTAR'?'selected':''}>Não foi possível testar</option><option value="EQUIPAMENTO_INOPERANTE" ${x.test_result==='EQUIPAMENTO_INOPERANTE'?'selected':''}>Equipamento inoperante</option><option value="OUTRO" ${x.test_result==='OUTRO'?'selected':''}>Outro</option></select><label>Observação / pendência</label><textarea name="notes" rows="2">${esc(x.notes||'')}</textarea><label class="secondary chipPhotoBtn">📷 Evidência<input name="photos" type="file" accept="image/*" capture="environment" multiple hidden></label><button type="submit">Registrar troca e concluir</button><small class="chipMsg"></small></form></article>`).join('')||'<p class="muted">Nenhum terminal neste filtro.</p>';document.querySelectorAll('.gForm').forEach(f=>f.onsubmit=save)}
async function save(e){e.preventDefault();let f=e.currentTarget,msg=f.querySelector('.chipMsg'),fd=new FormData(f),btn=f.querySelector('button');if(!fd.get('test_result')){msg.textContent='Selecione o resultado.';return}btn.disabled=true;try{let r=await fetch('/api/garage-chip-swaps/'+f.dataset.id,{method:'POST',body:fd}),j=await r.json();if(!r.ok)throw Error(j.error||'Falha');msg.textContent='Troca registrada.';await load()}catch(e){msg.textContent=e.message}finally{btn.disabled=false}}
['gCompany','gModel','gStatus'].forEach(id=>$(id).onchange=render);$('gTerminal').oninput=render;$('gClear').onclick=()=>{$('gCompany').value='';$('gTerminal').value='';$('gModel').value='';$('gStatus').value='';render()};load();
