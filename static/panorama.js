const $=id=>document.getElementById(id), esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let panData=[],current=null;
async function loadPan(){const d=await fetch('/api/panoramas',{cache:'no-store'}).then(r=>r.json());panData=d.locations||[];fillFilters();render()}
function fillFilters(){const fill=(id,vals)=>{const el=$(id),keep=el.value;el.innerHTML='<option value="">Todas</option>'+[...new Set(vals.filter(Boolean))].sort().map(v=>`<option>${esc(v)}</option>`).join('');el.value=keep};fill('panCompany',panData.map(x=>x.company));fill('panLine',panData.map(x=>x.line))}
function filtered(){const q=$('panSearch').value.toLowerCase(),c=$('panCompany').value,l=$('panLine').value,s=$('panStatus').value;return panData.filter(x=>(!q||x.location.toLowerCase().includes(q))&&(!c||x.company===c)&&(!l||x.line===l)&&(!s||x.status===s))}
function render(){const a=filtered();$('panTotal').textContent=a.length;$('panPending').textContent=a.filter(x=>x.status==='PENDENTE').length;$('panProgress').textContent=a.filter(x=>x.status==='EM ANDAMENTO').length;$('panDone').textContent=a.filter(x=>x.status==='CONCLUÍDA').length;$('panLocations').innerHTML=a.map(x=>`<button class="card panoramaLocation" data-id="${x.id}"><small>${esc(x.company)} · ${esc(x.line)}</small><b>${esc(x.location)}</b><span class="tag">${esc(x.status)}</span><small>${x.photo_count} foto(s) · ${x.points.length} ponto(s)</small></button>`).join('');document.querySelectorAll('.panoramaLocation').forEach(b=>b.onclick=()=>openLoc(+b.dataset.id))}
function openLoc(id){current=panData.find(x=>x.id===id);if(!current)return;$('panLocationId')&&($('panLocationId').value=id);$('panModalTitle').textContent=current.location;$('panModalMeta').textContent=`${current.company} · ${current.line} · ${current.status}`;renderPoints();$('panModal').classList.remove('hidden')}
function renderPoints(){$('panPoints').innerHTML=(current.points||[]).map(p=>`<section class="panPoint"><div class="sectionHead"><div><h3>${esc(p.name)}</h3><small>${p.photos.length} foto(s) · ${esc(p.status)}</small></div></div><div class="panGallery">${p.photos.map(ph=>`<figure><a href="${esc(ph.url)}" target="_blank"><img loading="lazy" src="${esc(ph.url)}"></a><figcaption>${esc(ph.uploaded_by)} · ${new Date(ph.created_at).toLocaleString('pt-BR')}${window.PAN_ROLE==='manager'?`<button class="dangerGhost" data-del="${ph.id}">Excluir</button>`:''}</figcaption></figure>`).join('')||'<p class="muted">Nenhuma foto.</p>'}</div></section>`).join('')||'<p class="muted">Nenhum ponto panorâmico iniciado.</p>';document.querySelectorAll('[data-del]').forEach(b=>b.onclick=async()=>{if(!confirm('Excluir esta foto?'))return;await fetch('/api/panoramas/photos/'+b.dataset.del,{method:'DELETE'});await loadPan();openLoc(current.id)})}
['panCompany','panLine','panStatus'].forEach(id=>$(id).onchange=render);$('panSearch').oninput=render;$('panClose').onclick=()=>$('panModal').classList.add('hidden');$('panCamera')&&($('panCamera').onclick=()=>$('panCameraInput').click());$('panCameraInput')&&($('panCameraInput').onchange=e=>{if(e.target.files?.length){const dt=new DataTransfer();[...$('panPhotos').files,...e.target.files].forEach(f=>dt.items.add(f));$('panPhotos').files=dt.files}});$('panForm')&&($('panForm').onsubmit=async e=>{e.preventDefault();const fd=new FormData(e.currentTarget);const files=$('panPhotos').files;fd.delete('photos');[...files].forEach(f=>fd.append('photos',f));if(navigator.geolocation)await new Promise(res=>navigator.geolocation.getCurrentPosition(p=>{fd.set('latitude',p.coords.latitude);fd.set('longitude',p.coords.longitude);res()},()=>res(),{timeout:3000}));const r=await fetch(`/api/panoramas/${$('panLocationId').value}/points`,{method:'POST',body:fd});const d=await r.json();if(!r.ok){alert(d.error||'Falha ao salvar.');return}e.currentTarget.reset();const id=current.id;await loadPan();openLoc(id)});loadPan();
